import styles from "./Dashboard.module.scss";

export const DashboardMarketBalance = () => {
  return (
    <div className={styles["dashboard-market-balance"]}>
      DashboardMarketBalance
    </div>
  );
};
