// import { MarketIndicatorData } from "@project/shared-types";
// import { MOCK_MARKET_INDICATORS } from "./mock-data-sets/market-indicators.mock";

// export const getMarketIndicators = async (): Promise<MarketIndicatorData[]> => {
//   await new Promise((resolve) => setTimeout(resolve, 1000));
//   const randomSetNumber: number = Math.floor(Math.random() * 10);
//   return MOCK_MARKET_INDICATORS[randomSetNumber];
// };
